id: atb-bus-times
name: Bus Times Norway
desc: Realtime bus departures for any stop in Norway.
author: jakobbbb
summary: Bus departures in Norway
category: Transportation
filename: atb_bus_times.star
type: app
config:
  - name: stop_id
    type: string
    desc: The Entur stop ID (e.g., NSR:StopPlace:6286 for Colletts gate).
    required: true
  - name: quay_id
    type: string
    desc: The Entur quay ID (e.g., NSR:Quay:11544 for Colletts gate).
    required: true
  - name: stop_name
    type: string
    desc: Display name for the stop (optional; will use API name if blank).
    required: false
  - name: num_departures
    type: int
    desc: Number of departures to show (1-5).
    default: 3
    min: 1
    max: 5
entrypoint: main
tags:
  - transportation
  - norway
  - bus
  - real-time
  - public-transport
  - travel
  - commute
  - schedule 